# 04 异常与处理记录

> 记录开发过程中所有异常：现象、原因分析、处理过程、结果与影响。
> 对应报告"7.2 问题修复与回归验证（如有）"及"6.3 重要调整记录"。

| 问题编号 | 发现的问题 | 原因分析 | 修改内容 | 受影响范围 | 处理结果/回归验证 |
|---|---|---|---|---|---|
| BUG-01 | `git push` 返回 502 Bad Gateway（CONNECT tunnel failed） | GitHub 服务端临时故障（网络层 502），非本地配置问题 | 等待后重试；先用 `git fetch` 验证 DNS/认证/网络链路正常 | 远程推送延迟 | 重试成功，远程更新至目标 commit |
| BUG-02 | `git push --force-with-lease` 报 `stale info`，强制推送被拒 | 本地远程跟踪引用（refs/remotes/origin/main）未正确建立（fetch 显示 [new branch]），lease 期望值不匹配 | 单人仓库场景改用 `git push --force`（无他人提交风险） | 仅推送方式，无数据影响 | 推送成功（`2922279...a20578d (forced update)`） |
| BUG-03 | GitHub 页面 commit 作者显示为 28468 而非 Num1AD | 首次 `git config user.name` 使用 Windows 用户名占位，commit 创建时作者已固化 | `git commit --amend --author="Num1AD <310890967+Num1AD@users.noreply.github.com>"` 改写作者，force push 同步 | commit hash 变化（2922279→a20578d），远程历史被重写 | 作者署名正确显示 Num1AD；hash 变化验证一致 |
| BUG-04 | 用户勾选"隐私保护"后未见隐藏邮箱显示 | 隐私保护已生效（页面顶部提示 web 操作使用 noreply 邮箱）；用户将"主邮箱仍显示在设置页"误判为未生效 | 解读页面：设置页显示主邮箱（Private 标签）属正常；noreply 邮箱为 310890967+Num1AD@users.noreply.github.com | 无 | 采用 noreply 邮箱完成身份配置 |
| BUG-05 | `git push` 反复失败：CONNECT tunnel failed 502 / schannel server closed | WorkBuddy 本地代理（127.0.0.1:6260）到 github.com 的隧道失效；git 继承环境变量代理导致推送失败；curl 走代理无响应、绕代理直连 200 OK | 对 github.com 关闭代理：`git config http.https://github.com/.proxy ""`（仅该域名生效） | 推送延迟，本地提交不受影响 | 配置后 push 立即成功（1399498..048a5a2） |
| BUG-06 | 报告架构图多处文字溢出方框外（层内说明文字左移越界、模块描述文字超框宽） | 绘制脚本用"文本框中心 x"做水平居中，但估算的文字宽度小于实际渲染宽度，导致左边界超出边框 | 层内说明文字改为标题行右侧左对齐；模块描述文案拆为三行并加高模块框；同步调整层高 | 仅报告 4.1 插图 | 重新生成后文字全部归位于框内（200 DPI 复核） |
| BUG-07 | 报告流程图左侧"拒绝"分支框被画布边界截断、旁支回环线与主干重叠、"是"分支标注重复 | 学生流程纵向布局预留高度不足；回环折线坐标与主干箭头重合 | 重写学生流程布局（画布加高至 1200、拒绝分支右移入界内、回环改走右侧通道、分支标注唯一化） | 仅报告 4.2 插图 | 重新生成后流程无截断、无重叠，分支判断可读 |
| BUG-08 | 界面截图脚本"报名"按钮选择器误匹配到"不可报名"按钮，截图显示报名未生效 | CSS 选择器 `:has-text("报名")` 为子串匹配，同时命中"不可报名"等禁用按钮 | 选择器改为精确匹配状态为"报名中"的活动卡片内按钮，并在报名后 sleep 等待状态刷新 | 仅截图脚本 | 重跑后学生端正确显示 2 条已报名记录（红色"取消报名"） |
| BUG-09 | 用 Word COM 把报告导出 PDF 做排版自检时报"文件可能已经损坏"，无法生成 PDF | **非文档问题**：用未修改的原始报告做对照组，同样报错；判定为本机 Office COM 自动化环境限制（静默模式打开即失败） | 改用 ZIP+XML 完整性严格校验替代 PDF 自检（zipfile.testzip 无损坏、12 个 XML 部件全部合法、5 张媒体嵌入正确） | 仅本地自检手段，不影响交付文件 | 校验通过；报告文件结构健康，可用 Word/WPS 正常打开 |
| BUG-10 | 插图脚本重复执行时锚点错位（第二次运行把正文标题当成了正文段落） | 脚本用硬编码段落序号 `[24]/[27]/[39]` 定位锚点，插入后序号整体漂移，重复运行即错位 | 改为**按正文内容前缀定位锚点**，并增加幂等保护（检测到已存在"图 4-1"图题即中止） | 插图脚本 insert_figures.py | 重跑锚点命中正确；正式报告插入一次成功、结果可复现 |
| BUG-11 | 前端没有「编辑活动」入口，但 README、docs/01(REQ-04)、docs/04 模块表、报告 4.2/6.1 正文与图 4-2 均声称教师可编辑活动 | 后端 PUT /api/activities/{id} 与 TEST-09/10 已实现，前端漏做入口，属于"文档有、界面无" | 前端新增编辑弹层与 openEdit/submitEdit；「我的活动」教师卡片新增「编辑」按钮，仅状态为"报名中"时显示 | 教师端活动管理（frontend/index.html、app.js） | 实测通过：3 个活动均出现编辑按钮；弹层标题正确、表单预填原值无误；提交成功并自动刷新列表 |
| BUG-12 | 报告 6.2 提交表序号跳号（11 直接到 13），且表格末尾残留一行全空单元格 | 此前增删提交行时未重排序号，删除后未清空行 | 将第 13 行序号改为 12；删除末尾空行 | 报告 6.2 关键 Git 提交记录 | 复核：表为 13 行（表头 + 12 条），序号 1~12 连续、无空行 |
| BUG-13 | project-data/03 称"全部 23 条提交"，而仓库实际有 28 条；且与 project-data/01 时间线（提到 071e3fb）自相矛盾 | 09-06、09-08、09-09 期间共 5 次提交未回填到提交表 | 提交表补全至 28 条；实验信息时间线补 09-06 / 09-08 / 09-09 / 09-11 条目 | project-data/01、03 | 与 `git log` 逐条复核一致 |
| BUG-14 | frontend/index.html 残留 118 处 `data-page-node-id` 属性（页面生成器写入的节点标识，无任何功能作用） | 早期用可视化页面工具生成页面时写入，后续未清理 | 用正则清除全部该属性 | frontend/index.html | 复核残留 0；`<div>/<template>/<form>` 标签配对与 Vue 指令数量均正常，页面功能未受影响 |
| BUG-15 | 编辑接口缺三类校验：可编辑"已开始（已截止）"的活动、人数上限可改到小于已报名数、开始时间可改到过去 | 发布接口有这三类校验，编辑接口未同步（入口不对称） | update_activity 增加：已开始的活动禁止编辑；按"修改后的最终值"校验"结束晚于开始、开始晚于当前"；人数上限不得小于已报名人数 | 活动模块 PUT 接口（backend/routers/activities.py） | 实测：三类非法输入均返回 400 与明确原因；正常编辑（改地点）返回 200，未误伤 |
| BUG-16 | `start_time`/`end_time` 无格式校验，非法格式可入库 | 两个字段是自由字符串，先后关系只靠字符串比较判断；`calc_status` 的 strptime 解析失败后保守返回"已截止" | 发布与编辑接口加 Pydantic `field_validator`：正则保证 `YYYY-MM-DD HH:MM` 书写格式统一（挡住 "9:30" 与斜杠日期），strptime 保证日期真实存在（挡住 2026-13-45） | 活动模块发布与编辑接口 | 实测：斜杠日期 / 非零填充 / 带秒 / 不存在的日期 四种输入均 422；标准格式正常发布 |
| BUG-17 | 退出登录只清前端 token，服务端仍认旧凭证（被窃取的 token 无法通过"退出"作废） | 服务端 `TOKENS` 表只有定义、写入、读取三处，没有删除；前端 `logout()` 未调用后端 | `auth.py` 新增 `revoke_token()`；`users.py` 新增 `POST /api/logout`（幂等：无 token 或已失效也返回成功）；前端 `logout()` 改为先请求注销、再清本地，网络异常不阻塞本地清理 | 用户模块接口、前端退出流程 | 实测：点退出后浏览器 token 清除、顶部回到未登录；退出前的 token 调 `/api/me` 返回 401（服务端确已注销） |

## 本轮其他加固（非缺陷，提升稳健性）

1. `sqlite3.connect(..., check_same_thread=False)`：FastAPI 同步依赖在线程池执行、依赖收尾不保证同一线程，每个请求各持独立连接，关闭线程检查是安全的；
2. 报名事务由 `BEGIN` 改为 `BEGIN IMMEDIATE`：立刻取写锁，避免 deferred 事务"先读后写"时并发读到"未满"而相继写入造成超员；
3. `verify_password` 改用 `hmac.compare_digest` 做常量时间比较，消除摘要比对的时序侧信道；
4. 删除死代码 `auth._current_time()`；
5. 前端发布表单默认结束时间改为"开始 + 2 小时"（原先开始与结束同为默认值，直接点发布必被后端拒绝）。

## 经验沉淀

1. GitHub 502 多为服务端临时故障，先验证网络链路（fetch 成功即可排除本地问题）再重试；
2. 单人仓库 force push 用 `git push --force` 即可；多人协作必须用 `--force-with-lease` 防覆盖；
3. commit 作者在创建时固化，事后改需要 amend（改变 hash），如需改写务必在历史公开前完成；
4. Git 作者署名（user.name/email）与 GitHub 账号是两个独立概念，各自独立配置。
