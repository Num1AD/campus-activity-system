# 04 异常与处理记录

> 记录开发过程中所有异常：现象、原因分析、处理过程、结果与影响。
> 对应报告"7.2 问题修复与回归验证（如有）"及"6.3 重要调整记录"。

| 问题编号 | 发现的问题 | 原因分析 | 修改内容 | 受影响范围 | 处理结果/回归验证 |
|---|---|---|---|---|---|
| BUG-01 | `git push` 返回 502 Bad Gateway（CONNECT tunnel failed） | GitHub 服务端临时故障（网络层 502），非本地配置问题 | 等待后重试；先用 `git fetch` 验证 DNS/认证/网络链路正常 | 远程推送延迟 | 重试成功，远程更新至目标 commit |
| BUG-02 | `git push --force-with-lease` 报 `stale info`，强制推送被拒 | 本地远程跟踪引用（refs/remotes/origin/main）未正确建立（fetch 显示 [new branch]），lease 期望值不匹配 | 单人仓库场景改用 `git push --force`（无他人提交风险） | 仅推送方式，无数据影响 | 推送成功（`2922279...a20578d (forced update)`） |
| BUG-03 | GitHub 页面 commit 作者显示为 28468 而非 Num1AD | 首次 `git config user.name` 使用 Windows 用户名占位，commit 创建时作者已固化 | `git commit --amend --author="Num1AD <310890967+Num1AD@users.noreply.github.com>"` 改写作者，force push 同步 | commit hash 变化（2922279→a20578d），远程历史被重写 | 作者署名正确显示 Num1AD；hash 变化验证一致 |
| BUG-04 | 用户勾选"隐私保护"后未见隐藏邮箱显示 | 隐私保护已生效（页面顶部提示 web 操作使用 noreply 邮箱）；用户将"主邮箱仍显示在设置页"误判为未生效 | 解读页面：设置页显示主邮箱（Private 标签）属正常；noreply 邮箱为 310890967+Num1AD@users.noreply.github.com | 无 | 采用 noreply 邮箱完成身份配置 |
| BUG-05 | `git push` 反复失败：CONNECT tunnel failed 502 / schannel server closed | WorkBuddy 本地代理（127.0.0.1:6260）到 github.com 的隧道失效；git 继承环境变量代理导致推送失败；curl 走代理无响应、绕代理直连 200 OK | 对 github.com 关闭代理：`git config http.https://github.com/.proxy ""`（仅该域名生效） | 推送延迟，本地提交不受影响 | 配置后 push 立即成功（1399498..048a5a2） |

## 经验沉淀

1. GitHub 502 多为服务端临时故障，先验证网络链路（fetch 成功即可排除本地问题）再重试；
2. 单人仓库 force push 用 `git push --force` 即可；多人协作必须用 `--force-with-lease` 防覆盖；
3. commit 作者在创建时固化，事后改需要 amend（改变 hash），如需改写务必在历史公开前完成；
4. Git 作者署名（user.name/email）与 GitHub 账号是两个独立概念，各自独立配置。
